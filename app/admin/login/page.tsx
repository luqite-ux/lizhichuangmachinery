import Image from "next/image"

export default async function AdminLoginPage({ searchParams }: { searchParams: Promise<{ error?: string; reason?: string }> }) {
  const params = await searchParams
  const message = params.error || (params.reason === "unauthorized" ? "请先登录后再访问管理后台" : "")
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950 p-4"><div className="w-full max-w-md rounded-xl border border-slate-700 bg-slate-900 p-8 shadow-2xl"><Image src="/images/logo.png" alt="郑州利之创机械设备有限公司 Logo" width={56} height={56} className="mb-5 h-14 w-14 object-contain" /><h1 className="text-2xl font-semibold text-white">郑州利之创机械设备有限公司</h1><p className="mt-2 text-sm text-slate-300">环球网站管理后台</p>{message ? <p className="mt-5 rounded-md bg-red-950 px-3 py-2 text-sm text-red-200">{message}</p> : null}<form action="/api/auth/login" method="post" className="mt-6 space-y-4"><label className="block text-sm font-medium text-slate-200">邮箱<input name="email" type="email" required autoComplete="email" className="mt-2 h-11 w-full rounded-md border border-slate-600 bg-slate-950 px-3 text-white outline-none focus:border-blue-400" /></label><label className="block text-sm font-medium text-slate-200">密码<input name="password" type="password" required autoComplete="current-password" className="mt-2 h-11 w-full rounded-md border border-slate-600 bg-slate-950 px-3 text-white outline-none focus:border-blue-400" /></label><button className="h-11 w-full rounded-md bg-blue-500 font-semibold text-white hover:bg-blue-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-300">登录</button></form></div></div>
}
